import { Licao } from "../../../../../models/desafios/licao/licao";
import { Dificuldade } from "../../../../../models/desafios/dificuldade";

const CONTEUDO = [
    '# Singleton: variações e quando (não) usar',
    '',
    'Você já viu duas formas de garantir "uma instância controlada": o Singleton simples (uma pro programa inteiro) e o Multiton (uma por chave de negócio, escolhida por quem chama `getInstance(chave)`). Existe uma terceira forma, muito comum em aplicações maiores: o **Singleton Escopado**.',
    '',
    '## O que muda no Singleton Escopado',
    '',
    'No Multiton, **quem chama** `getInstance(chave)` escolhe a chave — "colmeia-norte", "colmeia-sul", o nome do banco, etc. No Singleton Escopado, a "chave" deixa de ser algo de negócio escolhido livremente e passa a ser o **escopo de execução atual** — um pedido HTTP, um componente, um módulo — controlado por quem gerencia esse escopo (um framework, um container de injeção de dependência), não por quem usa a instância.',
    '',
    'Isso quer dizer: dentro de um mesmo escopo, todo mundo que pedir aquela instância recebe **exatamente a mesma**. Mas assim que o escopo muda — outro pedido, outro componente — uma **instância diferente** entra em cena, mesmo sendo "a mesma classe".',
    '',
    '## Por que isso importa?',
    '',
    'Um Singleton clássico é ótimo pra um recurso que é único **de verdade**, em qualquer lugar do programa (um logger, por exemplo). Mas muita coisa que parece precisar ser "única" na verdade só precisa ser única **dentro de um contexto**: os dados da requisição atual, o estado de um componente específico na tela, a configuração carregada por um módulo específico.',
    '',
    'Se você usasse um Singleton clássico pra isso, o dado do usuário A vazaria pro usuário B — porque só existe **uma** instância pro programa inteiro, compartilhada por todo mundo, o tempo todo. O Singleton Escopado resolve isso: garante instância única **onde faz sentido**, sem misturar dados entre escopos diferentes.',
    '',
    '## Como funciona, passo a passo',
    '',
    '1. Alguém (o framework, o container de injeção de dependência) define quando um **escopo** começa e quando termina — por exemplo, "começa quando a requisição chega, termina quando a resposta é enviada".',
    '2. Dentro desse escopo, existe um registro de instâncias — na prática, a mesma estrutura do Multiton, só que a chave agora é o **escopo atual**, não uma chave de negócio escolhida por quem usa.',
    '3. Toda vez que alguém pede a instância **dentro do mesmo escopo**, recebe a mesma — igual ao Multiton.',
    '4. Quando o escopo termina, a instância associada a ele deixa de ser usada.',
    '',
    '## O fluxo, de um jeito simples',
    '',
    '```mermaid',
    'flowchart TD',
    '    R1[Pedido 1 chega] --> E1[Escopo 1 e criado]',
    '    E1 --> I1[Instancia unica do Escopo 1]',
    '    R2[Pedido 2 chega] --> E2[Escopo 2 e criado]',
    '    E2 --> I2[Instancia unica do Escopo 2]',
    '```',
    '',
    '## Como fica em código',
    '',
    '```java',
    'public class ContextoDeRequisicao {',
    '    // Mesma ideia do registro do Multiton — só que a chave é o escopo atual',
    '    private static final Map<String, ContextoDeRequisicao> registroPorEscopo = new HashMap<>();',
    '',
    '    private final String usuario;',
    '',
    '    private ContextoDeRequisicao(String usuario) {',
    '        this.usuario = usuario;',
    '    }',
    '',
    '    public static ContextoDeRequisicao getInstance(String idDaRequisicaoAtual, String usuario) {',
    '        if (!registroPorEscopo.containsKey(idDaRequisicaoAtual)) {',
    '            registroPorEscopo.put(idDaRequisicaoAtual, new ContextoDeRequisicao(usuario));',
    '        }',
    '        return registroPorEscopo.get(idDaRequisicaoAtual);',
    '    }',
    '',
    '    // Chamado pelo framework quando a requisição termina',
    '    public static void encerrarEscopo(String idDaRequisicaoAtual) {',
    '        registroPorEscopo.remove(idDaRequisicaoAtual);',
    '    }',
    '}',
    '```',
    '',
    'Repare que o código é **quase idêntico** ao Multiton da lição anterior — a diferença não está na estrutura, está em **quem escolhe a chave** e **quem controla o ciclo de vida** dela. No Multiton, isso é feito à mão, pelo próprio programador. No Singleton Escopado, isso normalmente é responsabilidade de um framework, que cria e encerra escopos automaticamente.',
    '',
    '## Comparando os três',
    '',
    '| Variação | Quantas instâncias existem | Quem escolhe a chave/escopo | Exemplo de uso |',
    '|---|---|---|---|',
    '| **Singleton simples** | Uma, pro programa inteiro | Ninguém — é sempre a mesma | Logger, configuração global |',
    '| **Multiton** | Uma por chave de negócio | Quem chama getInstance(chave) | Uma conexão por banco de dados diferente |',
    '| **Singleton Escopado** | Uma por escopo de execução | O framework/container que gerencia o escopo | Dados da requisição atual, estado de um componente |',
    '',
    '## Cuidado!',
    '',
    'O maior risco do Singleton Escopado é o escopo **vazar**: se a instância de um escopo não for descartada corretamente quando o escopo termina, o próximo escopo pode acabar enxergando dados de um escopo anterior — por exemplo, dados de um usuário aparecendo pra outro. É essencialmente o mesmo risco do Singleton simples (estado global compartilhado), só que reduzido às fronteiras do escopo, quando essas fronteiras são respeitadas.',
    '',
    '## Resumindo',
    '',
    '- **Singleton Escopado** = Multiton em que a chave é o **escopo de execução** (requisição, componente, módulo), controlado por um framework, não por quem usa.',
    '- A estrutura de código é a mesma do Multiton — o que muda é quem escolhe a chave e quem controla quando ela deixa de existir.',
    '- Continua valendo o princípio do Singleton simples: **uma instância, um ponto de acesso** — só que "uma instância" agora significa "uma por escopo", não "uma pro programa inteiro".',
].join('\n');

export const DESAFIO_LICAO_SINGLETON_003 = new Licao({
    id: "licao-singleton-003",
    dificuldade: Dificuldade.Dificil,
    grupo: "Criacionais",
    nivel: 3,
    padrao: "Singleton",
    conteudoMarkdown: CONTEUDO
});
